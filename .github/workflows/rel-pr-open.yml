##### AUTOMATICALLY GENERATED BY ambuda-runner. DO NOT MODIFY #####
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        sparse-checkout: '

          ar

          ambuda-runner'
    - env:
        SSH_HOST: ${{ secrets.SSH_HOST }}
        SSH_KEY: ${{ secrets.SSH_KEY }}
        SSH_KNOWN_HOSTS: ${{ secrets.SSH_KNOWN_HOSTS }}
      name: Exec
      run: '

        cd ${{ github.workspace }}

        `pwd`ar remotely build'
  pr_commented:
    if: ${{ github.event.issue.pull_request.opened || github.event.issue.pull_request.reopened
      }}
    name: PR comment
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Create comment
      uses: peter-evans/create-or-update-comment@v2
      with:
        body: Your pull request is open. Reviewers will review the pullrequest at
          the earliest.
        issue-number: ${{ github.event.pull_request.number }}
        reactions: '+1'
  stage:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        sparse-checkout: '

          ar

          ambuda-runner'
    - env:
        SSH_HOST: ${{ secrets.SSH_HOST }}
        SSH_KEY: ${{ secrets.SSH_KEY }}
        SSH_KNOWN_HOSTS: ${{ secrets.SSH_KNOWN_HOSTS }}
      name: Exec
      run: '

        cd ${{ github.workspace }}

        `pwd`ar remotely stage'
name: Build release image and deploy on staging environment
'on':
  pull_request:
    branches:
    - release
    types:
    - opened
    - reopened
    - synchronize
  workflow_dispatch: {}
